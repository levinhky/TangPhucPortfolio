import Container from "@/themes/Container";

import Calendar from "./components/Calendar";

const SchedulePage = () => {
  return (
    <Container id="schedule-page" style="my-10 esm:mt-0 esm:mb-5">
      <Calendar />
    </Container>
  );
};

export default SchedulePage;
